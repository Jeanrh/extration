"""Derivações caras, calculadas uma vez sobre o domínio pequeno.

Resolver camada por finding custa uma busca de substring por linha; resolver
por plugin custa o mesmo trabalho dezenas de milhares de vezes em vez de
centenas de milhares. O resultado vira tabela e o scoring faz JOIN.
"""
